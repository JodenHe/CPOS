<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scau.jodenhe.cpos.dao.RoleMapper">
  <resultMap id="BaseResultMap" type="com.scau.jodenhe.cpos.entity.Role">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="roleName" jdbcType="VARCHAR" property="roleName" />
    <result column="roleSign" jdbcType="VARCHAR" property="roleSign" />
    <result column="description" jdbcType="VARCHAR" property="description" />
  </resultMap>
  <sql id="sql_columns">
    id,roleName,roleSign,description
  </sql>
  <sql id="sql_where">
    <where>
      <if test="null != item.id and '' != item.id">and id = #{item.id}</if>
      <if test="null != item.roleName and '' != item.roleName">and roleName = #{item.roleName}</if>
      <if test="null != item.roleSign and '' != item.roleSign">and roleSign = #{item.roleSign}</if>
      <if test="null != item.description and '' != item.description">and description = #{item.description}</if>
	</where>
  </sql>
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role where id = #{id}
  </select>
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role <include refid="sql_where" />
  </select>
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role <include refid="sql_where" />
  </select>
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role <include refid="sql_where" /> limit #{page.startRow}, #{page.pageSize}
  </select>
  <sql id="sql_save_columns">
    insert ignore into role(
	  <if test="null != item.roleName"> roleName</if>
	  <if test="null != item.roleSign">, roleSign</if>
	  <if test="null != item.description">, description</if>
	) values
  </sql>
  <sql id="sql_save_values">
    (
	  <if test="null != item.roleName"> #{item.roleName}</if>
	  <if test="null != item.roleSign">, #{item.roleSign}</if>
	  <if test="null != item.description">, #{item.description}</if>
	)
  </sql>
  <insert id="save" keyProperty="item.id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  <sql id="sql_update">
    update role set id = #{item.id}
      <if test="null != item.roleName">, roleName = #{item.roleName}</if>
      <if test="null != item.roleSign">, roleSign = #{item.roleSign}</if>
      <if test="null != item.description">, description = #{item.description}</if>
	where id = #{item.id}
  </sql>
  <update id="update">
    <include refid="sql_update" />
  </update>
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  <delete id="delArray">
    delete from role where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  <delete id="delList">
    delete from role where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
</mapper>