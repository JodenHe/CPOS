<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scau.jodenhe.cpos.dao.SaleRejectOrderItemMapper">
  <resultMap id="BaseResultMap" type="com.scau.jodenhe.cpos.entity.SaleRejectOrderItem">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="rejectNo" jdbcType="VARCHAR" property="rejectNo" />
    <result column="itemId" jdbcType="VARCHAR" property="itemId" />
    <result column="quantity" jdbcType="INTEGER" property="quantity" />
    <result column="rejectPrice" jdbcType="DECIMAL" property="rejectPrice" />
    <result column="subTotal" jdbcType="DECIMAL" property="subTotal" />
    <result column="rejectReason" jdbcType="VARCHAR" property="rejectReason" />
  </resultMap>
  <sql id="sql_columns">
    id,rejectNo,itemId,quantity,rejectPrice,subTotal,rejectReason
  </sql>
  <sql id="sql_where">
    <where>
      <if test="null != item.id and '' != item.id">and id = #{item.id}</if>
      <if test="null != item.rejectNo and '' != item.rejectNo">and rejectNo = #{item.rejectNo}</if>
      <if test="null != item.itemId and '' != item.itemId">and itemId = #{item.itemId}</if>
      <if test="null != item.quantity and '' != item.quantity">and quantity = #{item.quantity}</if>
      <if test="null != item.rejectPrice and '' != item.rejectPrice">and rejectPrice = #{item.rejectPrice}</if>
      <if test="null != item.subTotal and '' != item.subTotal">and subTotal = #{item.subTotal}</if>
      <if test="null != item.rejectReason and '' != item.rejectReason">and rejectReason = #{item.rejectReason}</if>
	</where>
  </sql>
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_reject_order_item where id = #{id}
  </select>
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_reject_order_item <include refid="sql_where" />
  </select>
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_reject_order_item <include refid="sql_where" />
  </select>
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_reject_order_item <include refid="sql_where" /> limit #{page.startRow}, #{page.pageSize}
  </select>
  <sql id="sql_save_columns">
    insert into sale_reject_order_item(
	  <if test="null != item.rejectNo"> rejectNo</if>
	  <if test="null != item.itemId">, itemId</if>
	  <if test="null != item.quantity">, quantity</if>
	  <if test="null != item.rejectPrice">, rejectPrice</if>
	  <if test="null != item.subTotal">, subTotal</if>
	  <if test="null != item.rejectReason">, rejectReason</if>
	) values
  </sql>
  <sql id="sql_save_values">
    (
	  <if test="null != item.rejectNo"> #{item.rejectNo}</if>
	  <if test="null != item.itemId">, #{item.itemId}</if>
	  <if test="null != item.quantity">, #{item.quantity}</if>
	  <if test="null != item.rejectPrice">, #{item.rejectPrice}</if>
	  <if test="null != item.subTotal">, #{item.subTotal}</if>
	  <if test="null != item.rejectReason">, #{item.rejectReason}</if>
	)
  </sql>
  <insert id="save" keyProperty="item.id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  <sql id="sql_update">
    update sale_reject_order_item set id = #{item.id}
      <if test="null != item.rejectNo">, rejectNo = #{item.rejectNo}</if>
      <if test="null != item.itemId">, itemId = #{item.itemId}</if>
      <if test="null != item.quantity">, quantity = #{item.quantity}</if>
      <if test="null != item.rejectPrice">, rejectPrice = #{item.rejectPrice}</if>
      <if test="null != item.subTotal">, subTotal = #{item.subTotal}</if>
      <if test="null != item.rejectReason">, rejectReason = #{item.rejectReason}</if>
	where id = #{item.id}
  </sql>
  <update id="update">
    <include refid="sql_update" />
  </update>
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  <delete id="delArray">
    delete from sale_reject_order_item where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  <delete id="delList">
    delete from sale_reject_order_item where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
</mapper>