<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scau.jodenhe.cpos.dao.RolePermissionMapper">
  <resultMap id="BaseResultMap" type="com.scau.jodenhe.cpos.entity.RolePermission">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="roleId" jdbcType="BIGINT" property="roleId" />
    <result column="permissionId" jdbcType="BIGINT" property="permissionId" />
  </resultMap>
  <sql id="sql_columns">
    id,roleId,permissionId
  </sql>
  <sql id="sql_where">
    <where>
      <if test="null != item.id and '' != item.id">and id = #{item.id}</if>
      <if test="null != item.roleId and '' != item.roleId">and roleId = #{item.roleId}</if>
      <if test="null != item.permissionId and '' != item.permissionId">and permissionId = #{item.permissionId}</if>
	</where>
  </sql>
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role_permission where id = #{id}
  </select>
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role_permission <include refid="sql_where" />
  </select>
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role_permission <include refid="sql_where" />
  </select>
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from role_permission <include refid="sql_where" /> limit #{page.startRow}, #{page.pageSize}
  </select>
  <sql id="sql_save_columns">
    insert ignore into role_permission(
	  <if test="null != item.roleId"> roleId</if>
	  <if test="null != item.permissionId">, permissionId</if>
	) values
  </sql>
  <sql id="sql_save_values">
    (
	  <if test="null != item.roleId"> #{item.roleId}</if>
	  <if test="null != item.permissionId">, #{item.permissionId}</if>
	)
  </sql>
  <insert id="save" keyProperty="item.id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  <sql id="sql_update">
    update role_permission set id = #{item.id}
      <if test="null != item.roleId">, roleId = #{item.roleId}</if>
      <if test="null != item.permissionId">, permissionId = #{item.permissionId}</if>
	where id = #{item.id}
  </sql>
  <update id="update">
    <include refid="sql_update" />
  </update>
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  <delete id="delArray">
    delete from role_permission where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  <delete id="delList">
    delete from role_permission where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
</mapper>