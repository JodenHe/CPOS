<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scau.jodenhe.cpos.dao.SaleOrderMapper">
  <resultMap id="BaseResultMap" type="com.scau.jodenhe.cpos.entity.SaleOrder">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="saleOrderNo" jdbcType="VARCHAR" property="saleOrderNo" />
    <result column="customerNo" jdbcType="VARCHAR" property="customerNo" />
    <result column="saleDateTime" jdbcType="TIMESTAMP" property="saleDateTime" />
    <result column="total" jdbcType="DECIMAL" property="total" />
    <result column="operatorId" jdbcType="BIGINT" property="operatorId" />
    <result column="shopId" jdbcType="BIGINT" property="shopId" />
  </resultMap>
  <sql id="sql_columns">
    id,saleOrderNo,customerNo,saleDateTime,total,operatorId,shopId
  </sql>
  <sql id="sql_where">
    <where>
      <if test="null != item.id and '' != item.id">and id = #{item.id}</if>
      <if test="null != item.saleOrderNo and '' != item.saleOrderNo">and saleOrderNo = #{item.saleOrderNo}</if>
      <if test="null != item.customerNo and '' != item.customerNo">and customerNo = #{item.customerNo}</if>
      <if test="null != item.saleDateTime and '' != item.saleDateTime">and saleDateTime = #{item.saleDateTime}</if>
      <if test="null != item.total and '' != item.total">and total = #{item.total}</if>
      <if test="null != item.operatorId and '' != item.operatorId">and operatorId = #{item.operatorId}</if>
      <if test="null != item.shopId and '' != item.shopId">and shopId = #{item.shopId}</if>
	</where>
  </sql>
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_order where id = #{id}
  </select>
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_order <include refid="sql_where" />
  </select>
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_order <include refid="sql_where" />
  </select>
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from sale_order <include refid="sql_where" /> limit #{page.startRow}, #{page.pageSize}
  </select>
  <sql id="sql_save_columns">
    insert into sale_order(
	  <if test="null != item.saleOrderNo"> saleOrderNo</if>
	  <if test="null != item.customerNo">, customerNo</if>
	  <if test="null != item.saleDateTime">, saleDateTime</if>
	  <if test="null != item.total">, total</if>
	  <if test="null != item.operatorId">, operatorId</if>
	  <if test="null != item.shopId">, shopId</if>
	) values
  </sql>
  <sql id="sql_save_values">
    (
	  <if test="null != item.saleOrderNo"> #{item.saleOrderNo}</if>
	  <if test="null != item.customerNo">, #{item.customerNo}</if>
	  <if test="null != item.saleDateTime">, #{item.saleDateTime}</if>
	  <if test="null != item.total">, #{item.total}</if>
	  <if test="null != item.operatorId">, #{item.operatorId}</if>
	  <if test="null != item.shopId">, #{item.shopId}</if>
	)
  </sql>
  <insert id="save" keyProperty="item.id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  <sql id="sql_update">
    update sale_order set id = #{item.id}
      <if test="null != item.saleOrderNo">, saleOrderNo = #{item.saleOrderNo}</if>
      <if test="null != item.customerNo">, customerNo = #{item.customerNo}</if>
      <if test="null != item.saleDateTime">, saleDateTime = #{item.saleDateTime}</if>
      <if test="null != item.total">, total = #{item.total}</if>
      <if test="null != item.operatorId">, operatorId = #{item.operatorId}</if>
      <if test="null != item.shopId">, shopId = #{item.shopId}</if>
	where id = #{item.id}
  </sql>
  <update id="update">
    <include refid="sql_update" />
  </update>
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  <delete id="delArray">
    delete from sale_order where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  <delete id="delList">
    delete from sale_order where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
</mapper>