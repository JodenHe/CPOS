<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scau.jodenhe.cpos.dao.SizeMapper">
  <resultMap id="BaseResultMap" type="com.scau.jodenhe.cpos.entity.Size">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="type" jdbcType="VARCHAR" property="type" />
    <result column="script" jdbcType="VARCHAR" property="script" />
    <result column="createTime" jdbcType="TIMESTAMP" property="createTime" />
    <result column="state" jdbcType="TINYINT" property="state" />
  </resultMap>
  <sql id="sql_columns">
    id,name,type,script,createTime,state
  </sql>
  <sql id="sql_where">
    <where>
      <if test="null != item.id and '' != item.id">and id = #{item.id}</if>
      <if test="null != item.name and '' != item.name">and name = #{item.name}</if>
      <if test="null != item.type and '' != item.type">and type = #{item.type}</if>
      <if test="null != item.script and '' != item.script">and script = #{item.script}</if>
      <if test="null != item.createTime and '' != item.createTime">and createTime = #{item.createTime}</if>
      <if test="null != item.state and '' != item.state">and state = #{item.state}</if>
	</where>
  </sql>
  <select id="selectById" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from size where id = #{id}
  </select>
  <select id="selectOne" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from size <include refid="sql_where" />
  </select>
  <select id="selectList" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from size <include refid="sql_where" />
  </select>
  <select id="selectPage" resultMap="BaseResultMap">
    select <include refid="sql_columns" /> from size <include refid="sql_where" /> limit #{page.startRow}, #{page.pageSize}
  </select>
  <sql id="sql_save_columns">
    insert ignore into size(
	  <if test="null != item.name"> name</if>
	  <if test="null != item.type">, type</if>
	  <if test="null != item.script">, script</if>
	  <if test="null != item.createTime">, createTime</if>
	  <if test="null != item.state">, state</if>
	) values
  </sql>
  <sql id="sql_save_values">
    (
	  <if test="null != item.name"> #{item.name}</if>
	  <if test="null != item.type">, #{item.type}</if>
	  <if test="null != item.script">, #{item.script}</if>
	  <if test="null != item.createTime">, #{item.createTime}</if>
	  <if test="null != item.state">, #{item.state}</if>
	)
  </sql>
  <insert id="save" keyProperty="item.id" useGeneratedKeys="true">
    <include refid="sql_save_columns" /><include refid="sql_save_values" />
  </insert>
  <insert id="batchSave">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_save_columns" /><include refid="sql_save_values" />
	</foreach>
  </insert>
  <sql id="sql_update">
    update size set id = #{item.id}
      <if test="null != item.name">, name = #{item.name}</if>
      <if test="null != item.type">, type = #{item.type}</if>
      <if test="null != item.script">, script = #{item.script}</if>
      <if test="null != item.createTime">, createTime = #{item.createTime}</if>
      <if test="null != item.state">, state = #{item.state}</if>
	where id = #{item.id}
  </sql>
  <update id="update">
    <include refid="sql_update" />
  </update>
  <update id="batchUpdate">
    <foreach collection="list" index="index" item="item" open="" separator=";" close="">
	  <include refid="sql_update" />
	</foreach>
  </update>
  <delete id="delArray">
    delete from size where id in
	<foreach collection="array" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
  <delete id="delList">
    delete from size where id in
	<foreach collection="list" index="index" item="item" open="(" separator="," close=")">#{item}</foreach>
  </delete>
</mapper>